<UserControl x:Class="AnimationEditor.AnimationKeyframeEditor.EditorView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:s="http://schemas.singulink.com/xaml"
             xmlns:controls="clr-namespace:System.Windows.Controls;assembly=PresentationFramework"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d"
             d:DesignHeight="1200" d:DesignWidth="800">

    <UserControl.Resources>
        <controls:BooleanToVisibilityConverter x:Key="boolConv"/>
    </UserControl.Resources>

    <DockPanel LastChildFill="False">
        <Label  DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.Title}" FontSize="18" FontWeight="Bold"/>
        <Label  DockPanel.Dock="Top" FontSize="10" Content="{loc:Loc AnimKeyframe.DragBorderHint}"/>
        <Label  DockPanel.Dock="Top" FontSize="20" Foreground="Red" Visibility="{Binding IsDirty.Value, UpdateSourceTrigger=PropertyChanged, Converter={StaticResource boolConv}}" Content="{loc:Loc AnimKeyframe.WarningDirty}"/>
        <Grid  DockPanel.Dock="Top">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="20*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0"  Content="{loc:Loc AnimKeyframe.CurrentlyEditingFrame}" FontSize="18" FontWeight="Bold"/>
            <Label Grid.Column="1" Content="{Binding CurrentFrameNumber.Value, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" FontSize="18" FontWeight="Bold"/>
            <Label Grid.Column="2" Content="/" FontSize="18" FontWeight="Bold"/>
            <Label Grid.Column="3" Content="{Binding TotalFrameNumber.Value, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}" FontSize="18" FontWeight="Bold"/>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DockPanel>
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.AllowSelectAnimRoot}" IsChecked="{Binding AllowToSelectAnimRoot.Value, UpdateSourceTrigger=PropertyChanged}"  />
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.IncrementFrameAfterCopy}" IsChecked="{Binding IncrementFrameAfterCopyOperation.Value, UpdateSourceTrigger=PropertyChanged}"  />
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.SelectPreviousBonesOnFrameChange}" IsChecked="{Binding AutoSelectPreviousBonesOnFrameChange.Value, UpdateSourceTrigger=PropertyChanged}"  />
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.EnableIK}" IsChecked="{Binding EnableInverseKinematics.Value, UpdateSourceTrigger=PropertyChanged}"  IsEnabled="false"  />
                <Label Content="{loc:Loc AnimKeyframe.LastEndBonesChain}"/>
                <ComboBox  
                          IsEditable="True"
                          IsTextSearchEnabled="false"
                          DisplayMemberPath="BoneName"
                          Text="{Binding ModelBoneListForIKEndBone.Filter, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" 
                          ItemsSource="{Binding ModelBoneListForIKEndBone.Values}" 
                          IsEnabled="false" 
                          SelectedItem="{Binding ModelBoneListForIKEndBone.SelectedItem, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"/>
                <!-- "{Binding EnableInverseKinematics.Value}" -->
            </DockPanel>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{loc:Loc AnimKeyframe.FirstFrame}"  Click="{s:MethodBinding FirstFrame}" Margin="0,0,2,0"/>
            <Button Grid.Column="1" Content="{loc:Loc AnimKeyframe.PrevFrame}"  Click="{s:MethodBinding PrevFrame}"  Margin="2,0,2,0"/>
            <Button Grid.Column="2" Content="{loc:Loc AnimKeyframe.NextFrame}"  Click="{s:MethodBinding NextFrame}" Margin="2,0,0,0"/>
            <Button Grid.Column="3" Content="{loc:Loc AnimKeyframe.LastFrame}"  Click="{s:MethodBinding LastFrame}" Margin="2,0,0,0"/>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{loc:Loc AnimKeyframe.DuplicateFrame}"  Click="{s:MethodBinding DuplicateFrame}" Margin="0,0,2,0"/>
            <Button Grid.Column="1" Content="{loc:Loc AnimKeyframe.RemoveFrame}"  Click="{s:MethodBinding RemoveFrame}"  Margin="2,0,2,0"/>
            <Button Grid.Column="2" Content="{loc:Loc AnimKeyframe.ResetPose}"  Click="{s:MethodBinding ResetPose}" Margin="2,0,0,0"/>
            <Button Grid.Column="3" Content="{loc:Loc AnimKeyframe.UndoPose}"  Click="{s:MethodBinding UndoPose}" Margin="2,0,0,0"/>
        </Grid>


        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{loc:Loc AnimKeyframe.Select}"  Click="{s:MethodBinding EnterSelectMode}" Margin="0,0,2,0"/>
            <Button Grid.Column="1" Content="{loc:Loc AnimKeyframe.SelectPreviousBones}"  Click="{s:MethodBinding SelectPreviousBones}" Margin="0,0,2,0"/>
            <Button Grid.Column="2" Content="{loc:Loc AnimKeyframe.Move}"  Click="{s:MethodBinding EnterMoveMode}" Margin="0,0,2,0"/>
            <Button Grid.Column="3" Content="{loc:Loc AnimKeyframe.Rotate}"  Click="{s:MethodBinding EnterRotateMode}"  Margin="2,0,2,0" />
            <Button Grid.Column="4" Content="{loc:Loc AnimKeyframe.Scale}"  Click="{s:MethodBinding EnterScaleMode}" Margin="2,0,0,0"/>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,5">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DockPanel>
                <Label Content="{loc:Loc AnimKeyframe.OnPasteOrInterpolation}"/>
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.PastePosition}" IsChecked="{Binding PastePosition.Value, UpdateSourceTrigger=PropertyChanged}"  />
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.PasteRotation}" IsChecked="{Binding PasteRotation.Value, UpdateSourceTrigger=PropertyChanged}"  />
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.PasteScale}" IsChecked="{Binding PasteScale.Value, UpdateSourceTrigger=PropertyChanged}"  />
            </DockPanel>
        </Grid>
        
        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{loc:Loc AnimKeyframe.CopyPose}"  Click="{s:MethodBinding CopyCurrentPose}" Margin="2,0,0,0"/>
            <Button Grid.Column="1" Content="{loc:Loc AnimKeyframe.PastePose}"  Click="{s:MethodBinding PasteIntoCurrentFrame}" Margin="2,0,0,0" />
            <Button Grid.Column="2" Content="{loc:Loc AnimKeyframe.PastePoseSelectedNodes}"  Click="{s:MethodBinding PasteIntoSelectedCurrentNode}" Margin="2,0,0,0" />
            <Button Grid.Column="3" Content="{loc:Loc AnimKeyframe.CopyNPastePrevEdited}"  Click="{s:MethodBinding PastePreviousEditedNodesIntoCurrentPose}" Margin="2,0,0,0" />
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <DockPanel>
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="200"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>
                        <ColumnDefinition Width="100"/>

                    </Grid.ColumnDefinitions>

                    <Label Grid.Column="0" Content="{loc:Loc AnimKeyframe.AnimationDuration}"/>
                    <TextBox Grid.Column="1" PreviewTextInput="AnimationTextInputPreview" Text="{Binding FramesDurationInSeconds, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}"></TextBox>
                    <Label Grid.Column="2" Content="{loc:Loc AnimKeyframe.Seconds}"/>
                    <Button Grid.Column="3"  Click="{s:MethodBinding ResetDuration}" Content="{loc:Loc General.Reset}"/>
                    <Button Grid.Column="4" Click="{s:MethodBinding ApplyDuration}" Content="{loc:Loc General.Apply}"/>
                </Grid>
            </DockPanel>

        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
        </Grid>
        
        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <StackPanel>
                <Label Content="{loc:Loc AnimKeyframe.InRangeCopyOperationsLine1}"/>
                <Label Content="{loc:Loc AnimKeyframe.InRangeCopyOperationsLine2}"/>
            </StackPanel>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel>
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.CopyPasteMultipleFrames}" IsChecked="{Binding CopyMoreThanSingleFrame, UpdateSourceTrigger=PropertyChanged}"  />
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.DontWarnDifferentSkeletons}" IsChecked="{Binding DontWarnDifferentSkeletons.Value, UpdateSourceTrigger=PropertyChanged}"  />
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.DontWarnIncomingFramesBigger}" IsChecked="{Binding DontWarnIncomingFramesBigger.Value, UpdateSourceTrigger=PropertyChanged}"  />
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.PasteUsingFormBelow}" IsChecked="{Binding PasteUsingFormBelow, UpdateSourceTrigger=PropertyChanged}"  IsEnabled="{Binding CopyMoreThanSingleFrame}"/>
            </DockPanel>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="110"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="70"/>
                <ColumnDefinition Width="120"/>
            </Grid.ColumnDefinitions>
            <Label Grid.Column="0" Content="{loc:Loc AnimKeyframe.PasteLength}"/>
            <TextBox Grid.Column="1" PreviewTextInput="FrameNrPreviewTextInput" TextChanged="FrameNrCantBeEmpty" IsEnabled="{Binding CopyMoreThanSingleFrame}" Text="{Binding FrameNrLength}"></TextBox>
            <Button Grid.Column="2" Content="{loc:Loc AnimKeyframe.SetMax}"  Click="{s:MethodBinding SetFrameLengthMax}" Margin="2,0,0,0"/>
            <Button Grid.Column="3" Content="{loc:Loc AnimKeyframe.FromClipboard}"  Click="{s:MethodBinding SetFrameLengthFromClipboard}" Margin="2,0,0,0"/>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="{loc:Loc AnimKeyframe.CopyPoseGlobalClipboard}"  Click="{s:MethodBinding CopyPoseInRangeToClipboard}" Margin="2,0,0,0"/>
            <Button Grid.Column="1" Content="{loc:Loc AnimKeyframe.PastePoseGlobalClipboard}"  Click="{s:MethodBinding PastePoseInRangeFromClipboard}" Margin="2,0,0,0" />
            <Button Grid.Column="2" Content="{loc:Loc AnimKeyframe.PastePoseSelectedGlobalClipboard}"  Click="{s:MethodBinding PasteIntoInRangeSelectedCurrentNodeFromClipboard}" Margin="2,0,0,0" />
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <StackPanel>
                <Label Content="{loc:Loc AnimKeyframe.InterpolateBetweenFrame}"/>
                <StackPanel Orientation="Horizontal">
                    <Label Content="{loc:Loc AnimKeyframe.SelectedFrameA}"/>
                    <Label Content="{Binding SelectedFrameAInterpolation.Value, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"></Label>
                </StackPanel>
                <StackPanel Orientation="Horizontal">
                    <Label Content="{loc:Loc AnimKeyframe.SelectedFrameB}"/>
                    <Label Content="{Binding SelectedFrameBInterpolation.Value, UpdateSourceTrigger=PropertyChanged, Mode=OneWay}"></Label>
                </StackPanel>
                <StackPanel>
                    <Label Content="{loc:Loc AnimKeyframe.InterpolationDragSlider}"/>
                    <Label Content="{loc:Loc AnimKeyframe.InterpolationApplyHint}"/>
                    <Label Content="{loc:Loc AnimKeyframe.InterpolationPreviewHint}"/>
                </StackPanel>
            </StackPanel>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{loc:Loc AnimKeyframe.FirstFrame}"  Click="{s:MethodBinding FirstFrame}" Margin="0,0,2,0"/>
            <Button Grid.Column="1" Content="{loc:Loc AnimKeyframe.PrevFrame}"  Click="{s:MethodBinding PrevFrame}"   Margin="2,0,2,0"/>
            <Button Grid.Column="2" Content="{loc:Loc AnimKeyframe.NextFrame}"  Click="{s:MethodBinding NextFrame}"   Margin="2,0,0,0"/>
            <Button Grid.Column="3" Content="{loc:Loc AnimKeyframe.LastFrame}"  Click="{s:MethodBinding LastFrame}"   Margin="2,0,0,0"/>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Button Grid.Column="0" Content="{loc:Loc AnimKeyframe.SelectFrameA}"  Click="{s:MethodBinding SelectFrameAInterpolation}" Margin="0,0,2,0"/>
            <Button Grid.Column="1" Content="{loc:Loc AnimKeyframe.SelectFrameB}"  Click="{s:MethodBinding SelectFrameBInterpolation}" Margin="2,0,2,0"/>
            <Button Grid.Column="2" Content="{loc:Loc AnimKeyframe.ApplyInterpolationCurrent}"  Click="{s:MethodBinding ApplyInterpolationOnCurrentFrame}" Margin="2,0,0,0"/>
            <Button Grid.Column="3" Content="{loc:Loc AnimKeyframe.ApplyInterpolationAcrossAB}"  Click="{s:MethodBinding ApplyInterpolationAcrossFrames}" Margin="2,0,0,0"/>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <DockPanel>
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.LivePreviewInterpolation}" IsChecked="{Binding PreviewInterpolation, UpdateSourceTrigger=PropertyChanged}"  />
                <CheckBox DockPanel.Dock="Top" Content="{loc:Loc AnimKeyframe.InterpolationOnlySelectedBones}" IsChecked="{Binding InterpolationOnlyOnSelectedBones, UpdateSourceTrigger=PropertyChanged}"  />
            </DockPanel>
        </Grid>
        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="100"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="0" Content="{loc:Loc General.Reset}"  Click="{s:MethodBinding ResetInterpolationSlider}" Margin="2,0,0,0"/>
            <Slider Grid.Column="1" Minimum="0" Maximum="1" TickFrequency="0.1" Value="{Binding InterpolationValue, UpdateSourceTrigger=PropertyChanged}" IsSnapToTickEnabled="true" TickPlacement="BottomRight" >
                <Slider.LayoutTransform>
                    <ScaleTransform ScaleX="2" ScaleY="2"/>
                </Slider.LayoutTransform>
            </Slider>
        </Grid>

        <Grid DockPanel.Dock="Top" Margin="5,5,5,0">
            <Separator Style="{StaticResource {x:Static ToolBar.SeparatorStyleKey}}" />
        </Grid>

        <Button DockPanel.Dock="Top" Margin="5" Content="{loc:Loc General.Save}" Click="{s:MethodBinding Save}"/>
        <Button DockPanel.Dock="Top" Margin="5" Content="{loc:Loc AnimKeyframe.SaveAs}" Click="{s:MethodBinding SaveAs}"/>

    </DockPanel>
</UserControl>
