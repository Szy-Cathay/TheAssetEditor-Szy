<UserControl x:Class="AssetEditor.Views.MenuBarView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:AssetEditor.Views"
             xmlns:viewModels="clr-namespace:AssetEditor.ViewModels"
             xmlns:loc="clr-namespace:Shared.Ui.Common;assembly=Shared.Ui"
             mc:Ignorable="d" 
             d:DesignHeight="30" d:DesignWidth="800">

    <DockPanel>
        <Menu VerticalAlignment="Center"
              DockPanel.Dock="Left"
              Background="Transparent">
            <Menu.Resources>
                <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource {x:Type MenuItem}}"/>
            </Menu.Resources>

            <MenuItem Header="{loc:Loc MenuBar.File}" BorderThickness="1.5">

                <MenuItem Header="{loc:Loc MenuBar.File.NewPack}" Command="{Binding MenuBar.CreateNewPackFileCommand}"/>
                <MenuItem Header="{loc:Loc MenuBar.File.NewAnimPackWh3}" Command="{Binding MenuBar.CreateAnimPackWarhammer3Command}"/>
                <MenuItem Header="{loc:Loc MenuBar.File.NewAnimPack3K}" Command="{Binding MenuBar.CreateAnimPack3kCommand}"/>

                <Separator/>
                <MenuItem Header="{loc:Loc MenuBar.File.SaveActivePack}" Command="{Binding MenuBar.SaveActivePackCommand}"/>
                <Separator/>

                <MenuItem Header="{loc:Loc MenuBar.File.OpenPack}" Command="{Binding MenuBar.OpenPackFileCommand}"/>

                <MenuItem Header="{loc:Loc MenuBar.File.OpenRecentPacks}" ItemsSource="{Binding MenuBar.RecentPackFiles}">
                    <MenuItem.Style>
                        <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource {x:Type MenuItem}}">
                            <Style.Triggers>
                                <Trigger Property="HasItems" Value="False">
                                    <Setter Property="IsEnabled" Value="False" />
                                </Trigger>
                            </Style.Triggers>
                        </Style>
                    </MenuItem.Style>
                    <MenuItem.ItemContainerStyle>
                        <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource {x:Type MenuItem}}">
                            <Setter Property="Command" Value="{Binding Command}" />
                        </Style>
                    </MenuItem.ItemContainerStyle>
                    <MenuItem.ItemTemplate>
                        <HierarchicalDataTemplate DataType="{x:Type viewModels:RecentPackFileItem}">
                            <TextBlock Text="{Binding Header}"/>
                        </HierarchicalDataTemplate>
                    </MenuItem.ItemTemplate>
                </MenuItem>

                <MenuItem Header="{loc:Loc MenuBar.File.OpenGamePacks}">
                    <MenuItem Header="{loc:Loc MenuBar.File.OpenGamePacks.Atttila}" Command="{Binding MenuBar.OpenAttilaPacksCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.File.OpenGamePacks.RomeRemastered}" Command="{Binding MenuBar.OpenRomeRemasteredPacksCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.File.OpenGamePacks.ThreeKingdoms}" Command="{Binding MenuBar.OpenThreeKingdomsPacksCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.File.OpenGamePacks.Warhammer2}" Command="{Binding MenuBar.OpenWarhammer2PacksCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.File.OpenGamePacks.Warhammer3}" Command="{Binding MenuBar.OpenWarhammer3PacksCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.File.OpenGamePacks.Troy}" Command="{Binding MenuBar.OpenTroyPacksCommand}"/>
                </MenuItem>

                <Separator/>
                <MenuItem Header="{loc:Loc MenuBar.File.Settings}" Command="{Binding MenuBar.OpenSettingsWindowCommand}"/>

                <Separator/>

                <MenuItem Header="{loc:Loc MenuBar.File.OpenAssetEditorFolder}" Command="{Binding MenuBar.OpenAssetEditorFolderCommand}"/>
                <MenuItem Header="{loc:Loc MenuBar.File.ClearAssetEditorFolder}" Command="{Binding MenuBar.ClearAssetEditorFolderCommand}"/>

                <Separator/>

                <MenuItem Header="{loc:Loc MenuBar.File.Exit}" />
            </MenuItem>

            <MenuItem Header="{loc:Loc MenuBar.View}" BorderThickness="1.5">
                <MenuItem Header="{loc:Loc MenuBar.View.TogglePackFileBrowser}" Command="{Binding MenuBar.TogglePackFileExplorerCommand}"/>
            </MenuItem>

            <MenuItem Header="{loc:Loc MenuBar.Tools}" BorderThickness="1.5" ItemsSource="{Binding MenuBar.Editors}">

                <MenuItem.ItemContainerStyle>
                    <Style TargetType="{x:Type MenuItem}" BasedOn="{StaticResource {x:Type MenuItem}}">
                        <Setter Property="Command" Value="{Binding OpenEditorCommand}" />
                        <Setter Property="IsEnabled" Value="{Binding IsEnabled}" />
                    </Style>
                </MenuItem.ItemContainerStyle>

                <MenuItem.ItemTemplate>
                    <HierarchicalDataTemplate DataType="{x:Type viewModels:EditorShortcutViewModel}">
                        <TextBlock Text="{Binding DisplayName}"/>
                    </HierarchicalDataTemplate>
                </MenuItem.ItemTemplate>

            </MenuItem>

            <MenuItem Header="{loc:Loc MenuBar.Reports}" BorderThickness="1.5">
                <MenuItem Header="{loc:Loc MenuBar.Reports.Rmv2}" Command="{Binding MenuBar.GenerateRmv2ReportCommand}"/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.MetaData}" Command="{Binding MenuBar.GenerateMetaDataReportCommand}"/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.FileList}" Command="{Binding MenuBar.GenerateFileListReportCommand}"/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.MetaJsons}" Command="{Binding MenuBar.GenerateMetaDataJsonsReportCommand}"/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.Materials}" Command="{Binding MenuBar.GenerateMaterialReportCommand}"/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.DialogueEventsWithStateGroups}" Command="{Binding MenuBar.GenerateDialogueEventInfoPrinterReportCommand}"/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.DialogueEventAndEventNames}" Command="{Binding MenuBar.GenerateDialogueEventAndEventNamePrinterReportCommand}"/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.DatDump}" Command="{Binding MenuBar.GenerateDatDumperReportCommand}"/>
                <Separator/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.Search}" Command="{Binding MenuBar.SearchCommand}"/>
                <Separator/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.TouchedFiles}">
                    <MenuItem Header="{loc:Loc MenuBar.Reports.TouchedFiles.StartRecorder}" Command="{Binding MenuBar.TouchedFileRecorderStartCommand }"/>
                    <MenuItem Header="{loc:Loc MenuBar.Reports.TouchedFiles.PrintFiles}" Command="{Binding MenuBar.TouchedFileRecorderPrintCommand }"/>
                    <MenuItem Header="{loc:Loc MenuBar.Reports.TouchedFiles.ExtractToPack}" Command="{Binding MenuBar.TouchedFileRecorderExtractCommand }"/>
                    <MenuItem Header="{loc:Loc MenuBar.Reports.TouchedFiles.StopRecorder}" Command="{Binding MenuBar.TouchedFileRecorderStopCommand }"/>
                </MenuItem>
                <Separator/>
                <MenuItem Header="{loc:Loc MenuBar.Reports.DebugClearConsole}" Command="{Binding MenuBar.ClearConsoleCommand}" />
                <MenuItem Header="{loc:Loc MenuBar.Reports.DebugPrintScopes}" Command="{Binding MenuBar.PrintScopeCommand}" />
            </MenuItem>

            <MenuItem Header="{loc:Loc MenuBar.Help}" BorderThickness="1.5">
                <MenuItem Header="{loc:Loc MenuBar.Help.ModdingWiki}" Command="{Binding MenuBar.OpenHelpCommand}"/>

                <Separator/>

                <MenuItem Header="{loc:Loc MenuBar.Help.VideoTutorials}">
                    <MenuItem Header="{loc:Loc MenuBar.Help.VideoTutorials.Basics1}" Command="{Binding MenuBar.OpenAssetEdBasic0TutorialCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.Help.VideoTutorials.Basics2}" Command="{Binding MenuBar.OpenAssetEdBasic1TutorialCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.Help.VideoTutorials.SkragReskin}" Command="{Binding MenuBar.OpenSkragTutorialCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.Help.VideoTutorials.TzarGuardWithGun}" Command="{Binding MenuBar.OpenTzarGuardTutorialCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.Help.VideoTutorials.KostaltynReskin}" Command="{Binding MenuBar.OpenKostalynTutorialCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.Help.VideoTutorials.RecolouringModels}" Command="{Binding MenuBar.OpenRecolouringModelsTutorialCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.Help.VideoTutorials.AnimationBasics}" Command="{Binding MenuBar.OpenAnimationBasicsTutorialCommand}"/>
                    <MenuItem Header="{loc:Loc MenuBar.Help.VideoTutorials.AnimatedPropsAndCustomPacks}" Command="{Binding MenuBar.OpenAnimatedPropTutorialCommand}"/>
                </MenuItem>

                <Separator/>

                <MenuItem Header="{loc:Loc MenuBar.Help.Discord}" Command="{Binding MenuBar.OpenDiscordCommand}"/>
            </MenuItem>
        </Menu>
    </DockPanel>
</UserControl>